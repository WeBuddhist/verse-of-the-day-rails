Read `4-SYSTEM/Skills/source-property-extractor/SKILL.md` in full, then execute it on the file(s) or input the user specifies.

Skill purpose: Extracts structured metadata from a source file and writes it to frontmatter.
